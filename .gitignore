/target

# Local config generated by `init-config` — may name markets you watch
rustle.toml

# Collected market data (Parquet partitions)
/data

# Tooling caches / local agent config
/.serena
/.claude

# Local-only design notes
/docs

# Local-only TDD artifacts
/.github/tdd
